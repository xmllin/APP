<Window x:Class="Nexora.Services.StyledMessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Уведомление"
        Width="480"
        SizeToContent="Height"
        MinHeight="190"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <Window.Resources>
        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="Height" Value="34"/>
            <Setter Property="MinWidth" Value="92"/>
            <Setter Property="Padding" Value="18,6"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="Foreground" Value="#F1F6FF"/>
            <Setter Property="Background" Value="#173250"/>
            <Setter Property="BorderBrush" Value="#31516E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#21496F"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#4C9AEB"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#285C8D"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Cursor" Value="Arrow"/>
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DialogPrimaryButton" TargetType="Button" BasedOn="{StaticResource DialogButton}">
            <Setter Property="Background" Value="#176BE0"/>
            <Setter Property="BorderBrush" Value="#3D8FE8"/>
        </Style>
    </Window.Resources>

    <Border Background="#0B192A"
            BorderBrush="#2A4A69"
            BorderThickness="1"
            CornerRadius="14"
            Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="44"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border x:Name="IconHost"
                    Width="36"
                    Height="36"
                    CornerRadius="10"
                    Background="#176BE0"
                    VerticalAlignment="Top">
                <TextBlock x:Name="IconText"
                           Text="i"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           TextAlignment="Center"/>
            </Border>

            <StackPanel Grid.Column="1" Grid.ColumnSpan="2" Margin="12,0,0,0">
                <TextBlock x:Name="CaptionText"
                           FontSize="17"
                           FontWeight="SemiBold"
                           Foreground="#F1F6FF"
                           TextWrapping="Wrap"/>
                <TextBlock x:Name="MessageText"
                           Margin="0,7,0,0"
                           FontSize="13"
                           Foreground="#B9CAE2"
                           TextWrapping="Wrap"
                           MaxWidth="400"/>
            </StackPanel>

            <StackPanel Grid.Row="2"
                        Grid.Column="1"
                        Grid.ColumnSpan="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,18,0,0">
                <Button x:Name="OkButton"
                        Content="OK"
                        Click="OkButton_Click"
                        Style="{StaticResource DialogPrimaryButton}"/>
                <Button x:Name="YesButton"
                        Content="Продолжить"
                        Click="YesButton_Click"
                        Style="{StaticResource DialogPrimaryButton}"
                        Visibility="Collapsed"/>
                <Button x:Name="NoButton"
                        Content="Отмена"
                        Click="NoButton_Click"
                        Style="{StaticResource DialogButton}"
                        Visibility="Collapsed"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
<Window x:Class="Nexora.Services.LibraryDetailsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="620" Height="560" MinWidth="520" MinHeight="440"
        WindowStartupLocation="CenterOwner" WindowStyle="None" ResizeMode="CanResize"
        AllowsTransparency="True" Background="Transparent" ShowInTaskbar="False"
        UseLayoutRounding="True" SnapsToDevicePixels="True">
    <Window.Resources>
        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="Height" Value="36"/><Setter Property="MinWidth" Value="96"/><Setter Property="Padding" Value="18,7"/>
            <Setter Property="Background" Value="#176BE0"/><Setter Property="BorderBrush" Value="#3D8FE8"/><Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="White"/><Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value><ControlTemplate TargetType="Button">
                    <Border x:Name="B" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="B" Property="Background" Value="#2B82EF"/><Setter TargetName="B" Property="BorderBrush" Value="#72B8FF"/></Trigger>
                        <Trigger Property="IsPressed" Value="True"><Setter TargetName="B" Property="Background" Value="#0E5CBD"/></Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate></Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="Label" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#7FA7D1"/><Setter Property="FontSize" Value="12"/><Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,16,0"/>
        </Style>
        <Style x:Key="Value" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#EAF3FF"/><Setter Property="FontSize" Value="12"/><Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </Window.Resources>
    <Border Background="#0B192A" BorderBrush="#2A4A69" BorderThickness="1" CornerRadius="14" Padding="20">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock x:Name="TitleText" FontSize="20" FontWeight="SemiBold" Foreground="#F1F6FF" TextWrapping="Wrap"/>
                    <TextBlock Text="Сведения о компоненте" Foreground="#7FA7D1" FontSize="12" Margin="0,4,0,0"/>
                </StackPanel>
                <Button Grid.Column="1" Content="Закрыть" Click="Close_Click" Style="{StaticResource DialogButton}"/>
            </Grid>
            <ScrollViewer Grid.Row="1" Margin="0,18,0,12" VerticalScrollBarVisibility="Auto">
                <Grid x:Name="DetailsGrid" Margin="0,0,6,0">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="150"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                </Grid>
            </ScrollViewer>
            <TextBlock Grid.Row="2" Text="Все значения определяются по данным конфигурации и обнаружению компонента." Foreground="#6888AB" FontSize="10"/>
        </Grid>
    </Border>
</Window>
<UserControl x:Class="WpfApp1.Pages.AppDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:WpfApp1.Services"
    Background="Transparent">
    <UserControl.Resources>
        <local:AppIconConverter x:Key="AppIconConverter"/>
        <local:CategoryColorConverter x:Key="CategoryColorConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style x:Key="CopyableDetailsText" TargetType="TextBox">
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>
        <Style x:Key="VersionComboBoxItem" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#EEF5FF"/>
            <Setter Property="Background" Value="#0F2035"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="VersionItemBorder" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="VersionItemBorder" Property="Background" Value="#173250"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="VersionItemBorder" Property="Background" Value="#1468D8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <DataTemplate x:Key="VersionItemTemplate">
            <TextBlock Text="{Binding DisplayVersion}" TextTrimming="CharacterEllipsis" />
        </DataTemplate>
        <Style x:Key="VersionComboBox" TargetType="ComboBox">
            <Style.Resources>
                <Style x:Key="VersionDropDownToggle" TargetType="ToggleButton">
                    <Setter Property="Focusable" Value="False"/>
                    <Setter Property="IsTabStop" Value="False"/>
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ToggleButton">
                                <Border Background="Transparent" BorderThickness="0">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Style.Resources>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Foreground" Value="#EEF5FF"/>
            <Setter Property="Background" Value="#0F2035"/>
            <Setter Property="BorderBrush" Value="#24415F"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource VersionComboBoxItem}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                                <Border x:Name="VersionField" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="9" Padding="{TemplateBinding Padding}"
                                    PreviewMouseLeftButtonDown="VersionField_PreviewMouseLeftButtonDown">
                                <Grid>
                                    <ContentPresenter Margin="2,0,30,0" VerticalAlignment="Center"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"/>
                                    <ToggleButton x:Name="DropDownToggle" HorizontalAlignment="Right" Width="30"
                                                  IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                                  ClickMode="Press" Style="{StaticResource VersionDropDownToggle}">
                                        <Path Width="11" Height="7" Stretch="None" Stroke="#D8E7FF" StrokeThickness="1.5"
                                              Data="M 0 0 L 5.5 5.5 L 11 0"/>
                                    </ToggleButton>
                                </Grid>
                            </Border>
                            <Popup x:Name="PART_Popup" AllowsTransparency="True" Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom"
                                   PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                   PopupAnimation="Fade" StaysOpen="False">
                                <Border MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        MaxHeight="260" Background="#0F2035" BorderBrush="#367FCE"
                                        BorderThickness="1" CornerRadius="8" Padding="2">
                                    <ScrollViewer x:Name="VersionScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                                                  CanContentScroll="False">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="VersionField" Property="BorderBrush" Value="#367FCE"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="VersionField" Property="BorderBrush" Value="#5AA4FF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="VersionField" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="32,24,32,42">
            <Border Background="Transparent" BorderThickness="0" CornerRadius="14" Padding="4,0">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="112"/><ColumnDefinition Width="*"/><ColumnDefinition Width="210"/></Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Top">
                        <Image Width="100" Height="100" Source="{Binding Icon, Converter={StaticResource AppIconConverter}}" Stretch="Uniform"/>
                        <TextBlock Text="{Binding Category}" Foreground="{Binding Category, Converter={StaticResource CategoryColorConverter}}" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="22,0,24,0">
                        <WrapPanel VerticalAlignment="Center">
                            <TextBlock Text="{Binding Name}" FontSize="30" FontWeight="Bold" Margin="0,0,8,0"/>
                            <Border Background="#FF0000" BorderBrush="#FF0000" BorderThickness="1" CornerRadius="7" Padding="7,2" VerticalAlignment="Center" Visibility="{Binding IsBlockedInRussia, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Заблокировано в РФ" Foreground="#FFFFFF" FontSize="12" FontWeight="Bold"/>
                                    <Image Source="{local:SvgImage Source=interface/white/exclamation-circle.svg}" Width="16" Height="16" Margin="7,0,0,0"/>
                                </StackPanel>
                            </Border>
                        </WrapPanel>
                        <TextBlock Text="{Binding Description}" Foreground="{StaticResource MutedBrush}" FontSize="14" TextWrapping="Wrap" MaxHeight="42" Margin="0,4,0,15"/>
                        <StackPanel Orientation="Horizontal" Height="24" VerticalAlignment="Center">
                            <Image Source="{local:SvgImage Source=interface/white/link.svg}" Width="16" Height="16" Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Официальный сайт — " Foreground="{StaticResource MutedBrush}" FontSize="13" VerticalAlignment="Center"/>
                            <Button Click="OpenWebsite_Click" Background="#01000000" BorderThickness="0" Padding="0" Cursor="Hand"
                                    Focusable="False" IsTabStop="False" KeyboardNavigation.IsTabStop="False"
                                    FocusVisualStyle="{x:Null}"
                                    VerticalAlignment="Center">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" Cursor="{TemplateBinding Cursor}">
                                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Website}" Foreground="#58AEFF" FontSize="13" FontWeight="SemiBold" IsHitTestVisible="False"/>
                                    <Image Source="{local:SvgImage Source=interface/white/external-link.svg}" Width="13" Height="13" Margin="6,0,0,0" IsHitTestVisible="False"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Grid.Column="2" VerticalAlignment="Top">
                        <ComboBox x:Name="VersionComboBox" ItemTemplate="{StaticResource VersionItemTemplate}" Style="{StaticResource VersionComboBox}" IsEnabled="False" Visibility="Collapsed" SelectionChanged="VersionComboBox_SelectionChanged" Margin="0,0,0,10"/>
                        <Button x:Name="DownloadButton" Click="Download_Click" Cursor="Hand" Style="{StaticResource PrimaryButton}" Margin="0,0,0,10" Padding="20,12" HorizontalAlignment="Stretch">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Image Source="{local:SvgImage Source=interface/white/download.svg}" Width="18" Height="18" Margin="0,0,8,0"/>
                                <TextBlock Text="Скачать"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource GhostButton}" Padding="20,12" HorizontalAlignment="Stretch">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Image Source="{local:SvgImage Source=interface/white/heart.svg}" Width="18" Height="18" Margin="0,0,8,0"/>
                                <TextBlock Text="В избранное"/>
                            </StackPanel>
                        </Button>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0" Visibility="{Binding IsBlockedInRussia, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Image Source="{local:SvgImage Source=interface/white/exclamation-circle.svg}" Width="16" Height="16" Margin="0,0,6,0"/>
                            <TextBlock Text="Может потребоваться VPN" Foreground="{StaticResource MutedBrush}" FontSize="11" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock x:Name="DownloadStatusText" Foreground="{StaticResource MutedBrush}" TextTrimming="CharacterEllipsis" TextAlignment="Center" Margin="0,6,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
            <Grid Margin="0,16,0,0">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <Border Background="#0D1B2E" BorderThickness="0" Padding="16" Margin="0,0,6,0" CornerRadius="12">
                    <StackPanel><StackPanel Orientation="Horizontal"><Border Width="37" Height="37" CornerRadius="10" Background="#18385E" Padding="8"><Image Source="{local:SvgImage Source=interface/white/info-circle.svg}" Stretch="Uniform"/></Border><StackPanel Margin="9,0,0,0"><TextBlock Text="Последняя версия" Foreground="{StaticResource MutedBrush}" FontSize="13" VerticalAlignment="Center"/><TextBox x:Name="ReleaseVersionText" Text="Не определена" Style="{StaticResource CopyableDetailsText}" FontSize="17" FontWeight="SemiBold" Margin="0"/></StackPanel></StackPanel></StackPanel>
                </Border>
                <Border Grid.Column="1" Background="#0D1B2E" BorderThickness="0" Padding="16" Margin="6,0" CornerRadius="12">
                    <StackPanel><StackPanel Orientation="Horizontal"><Border Width="37" Height="37" CornerRadius="10" Background="#18385E" Padding="8"><Image Source="{local:SvgImage Source=interface/white/folder.svg}" Stretch="Uniform"/></Border><StackPanel Margin="9,0,0,0"><TextBlock Text="Формат загрузки" Foreground="{StaticResource MutedBrush}" FontSize="13" VerticalAlignment="Center"/><TextBox x:Name="ReleaseFormatText" Text="Не указан" Style="{StaticResource CopyableDetailsText}" FontSize="17" FontWeight="SemiBold" Margin="0"/></StackPanel></StackPanel></StackPanel>
                </Border>
                <Border Grid.Column="2" Background="#0D1B2E" BorderThickness="0" Padding="16" Margin="6,0,0,0" CornerRadius="12">
                    <StackPanel><StackPanel Orientation="Horizontal"><Border Width="37" Height="37" CornerRadius="10" Background="#18385E" Padding="8"><Image Source="{local:SvgImage Source=interface/white/icons8-storage-50.svg}" Stretch="Uniform"/></Border><StackPanel Margin="9,0,0,0"><TextBlock Text="Вес файла" Foreground="{StaticResource MutedBrush}" FontSize="13" VerticalAlignment="Center"/><TextBox x:Name="ReleaseSizeText" Text="Не указан" Style="{StaticResource CopyableDetailsText}" FontSize="17" FontWeight="SemiBold" Margin="0"/></StackPanel></StackPanel></StackPanel>
                </Border>
            </Grid>
            <Border Background="#0D1B2E" BorderThickness="0" Padding="22" Margin="0,16,0,0" CornerRadius="12">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10"><Border Width="34" Height="34" CornerRadius="10" Background="#18385E" Padding="8"><Image Source="{local:SvgImage Source=interface/white/category-office.svg}" Stretch="Uniform"/></Border><TextBlock Text="Описание" FontSize="21" FontWeight="SemiBold" VerticalAlignment="Center" Margin="10,0,0,0"/></StackPanel>
                    <TextBox Text="{Binding LongDescription}" Style="{StaticResource CopyableDetailsText}" FontSize="15" Foreground="#C7D8ED"/>
                </StackPanel>
            </Border>
            <Button Click="Back_Click" Style="{StaticResource GhostButton}" HorizontalAlignment="Left" Margin="0,18,0,0">
                <StackPanel Orientation="Horizontal">
                    <Image Source="{local:SvgImage Source=interface/white/back.svg}" Width="16" Height="16" Margin="0,0,8,0"/>
                    <TextBlock Text="Назад"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </ScrollViewer>
</UserControl>
